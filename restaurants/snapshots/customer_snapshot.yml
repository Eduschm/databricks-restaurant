snapshots:
  - name: customer_snapshot
    relation: source('landing', 'customers')
    config:
      schema: snapshots
      unique_key: customer_id
      strategy: check
      check_cols: all
      hard_deletes: invalidate