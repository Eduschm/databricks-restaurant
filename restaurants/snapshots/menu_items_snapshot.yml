snapshots:
  - name: menu_items_snapshot
    relation: source('landing', 'menu_items')
    config:
      schema: snapshots
      unique_key: item_id
      strategy: check
      check_cols: all
      hard_deletes: invalidate