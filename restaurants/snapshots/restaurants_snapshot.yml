snapshots:
  - name: restaurants_snapshot
    relation: source('landing', 'restaurants')
    config:
      schema: snapshots
      unique_key: restaurant_id
      strategy: check
      check_cols: all
      hard_deletes: invalidate